# ZhuHai No.1 Middle School - Food Safety Inspection System
# Tencent Cloud Windows Server deployment
# Frontend port: 8082 | Backend API port: 3002
# Upload this file to Nginx conf directory and rename to zhuhaiyizhong.conf

map $http_origin $cors_allow_origin {
    default "";
    "http://159.75.106.179:8082" $http_origin;
    "http://localhost:8082"       $http_origin;
    "http://127.0.0.1:8082"      $http_origin;
}

server {
    # Use a dedicated frontend port when there is no private domain.
    # Keep existing system on 8080 and expose this system on 8082.
    listen 8082;
    server_name 159.75.106.179;

    # Path to static frontend assets (upload your index.html/js/css here)
    # Windows Server 路径，Nginx for Windows 使用正斜杠
    root C:/zhuhaiyizhong;
    index index.html;

    # -------- Frontend static hosting --------
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Long cache for versioned assets
    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|webp|woff|woff2|ttf)$ {
        try_files $uri =404;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable";
        access_log off;
    }

    # HTML should not be strongly cached
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # -------- API reverse proxy --------
    # Your Node API process should listen at 127.0.0.1:3002
    location /api/ {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 10s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;

        # CORS allowlist
        if ($cors_allow_origin != "") {
            add_header Access-Control-Allow-Origin $cors_allow_origin always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Allow-Methods "GET,POST,PUT,PATCH,DELETE,OPTIONS" always;
            add_header Access-Control-Allow-Headers "Authorization,Content-Type" always;
        }

        if ($request_method = OPTIONS) {
            return 204;
        }
    }

    # Optional API health pass-through
    location = /health {
        proxy_pass http://127.0.0.1:3002/health;
    }

    # Basic gzip for low bandwidth servers
    gzip on;
    gzip_types text/plain text/css application/json application/javascript application/xml+rss application/xml;
    gzip_min_length 1024;
}
