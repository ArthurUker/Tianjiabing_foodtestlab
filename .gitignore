# Dependencies
node_modules/
**/node_modules/

# Build outputs
coverage/
dist/
build/
*.tsbuildinfo

# Environment files
.env
.env.*
!.env.example
backend/.env
backend/.env.*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.vscode/settings.json

# Runtime / cache
.cache/
tmp/
temp/

# Local handoff / context notes (not for repo)
docs/HANDOFF.md

# Cypress debug artifacts
cypress/_dashstate.txt
cypress/_errcap_out.txt
cypress/e2e/_errcap.cy.js
cypress/screenshots/

# Local PostgreSQL (portable binary + data dir, for local testing only)
backend/pgsql/
pgsql.zip

# Local portable test scripts (local-only, not for repo)
portable-start.bat
portable-stop.bat

# Prisma
prisma/*.db
backend/prisma/*.db
backend/.env.local
.env.local
backend/prisma/prisma/*.db
*.bak
*.bak.*

# IDE local data
.codebuddy/
.ssh/
# npm cache (local build cache, not for repo)
.npm/

# 数据库备份目录（含加密备份 .aes / meta.json / 本地明文，均不入库）
backend/backups/
