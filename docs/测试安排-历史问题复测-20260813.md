# 历史问题复测安排（最终版）

> 生成日期：20260813（由 scripts/gen-retest-guide.mjs 从 testCaseDefs.js 自动生成）
> 数据来源：5 份测试反馈 Excel（吴晓君 / 曾水平 / 吴翠楠 / 浏览器测试验证 ×2）
> **本文件与 test-report.html 网页同步**——两者都从 backend/lib/testCaseDefs.js 读取，
> 修改只需改 defs，再运行本脚本重新生成即可，不会出现"一个更新一个老样子"。
> 环境：`http://111.231.166.161:8080/`
> 报告上报：`http://111.231.166.161:8080/test-report.html`（每项都有内联测试步骤，直接照做）

## 一、本次分工总览（28 项）

| 人员 | 负责条目 | 数量 |
|---|---|---|
| 吴翠楠 | R8 · R9 · R10 · R11 · R12 · R19 · R20 · R24 · R28 | 9 |
| 曾水平 | R3 · R5 · R6 · R13 · R25 · R26 · R27 | 7 |
| 吴晓君 | R1 · R2 · R4 · R7 · R14 · R15 · R16 · R17 · R18 · R21 · R22 · R23 | 12 |

> 说明：分工详见下方各用例标注。吴晓君负担较重（多平台超管侧），如任务过重可把
> R21/R22/R23（登录相关，简单）分给曾水平或吴翠楠协助。

## 二、各用例测试步骤指导

### R1 学校列表：无法删除已新增的学校（备注：只能停用不能删除）

> 负责人：吴晓君

**目的：** 确认学校列表能否删除新增加的学校。

**步骤：**
- 1.平台超管登录 admin-schools.html→学校列表。2.找一条测试用的新学校（不要动真实学校）。3.看该行有没有「删除」按钮。

**判定：** 有删除按钮且能删测试学校→通过；只有停用没有删除→失败并截图。备注：如果产品设计就是「只能停用不能删除」，实际表现写「确认是产品设计如此」，结果可填通过。

### R2 主界面：返回主界面后无法用任何管理员/学校账号登录

> 负责人：吴晓君

**目的：** 确认从管理后台「返回主界面」后能正常登录。

**步骤：**
- 1.平台超管登录 admin-schools.html。2.操作中点击「返回主界面」。3.看跳转后的页面能否用管理员或学校账号登录。

**判定：** 能正常打开登录页/主界面并登录→通过；打开后登录失败/白屏/无法登录→失败并截图。备注：记录你点的是什么入口、跳到什么地址。

### R3 数据同步：云端同步/暂停同步/清空本地缓存后无法点击取消

> 负责人：曾水平

**目的：** 确认数据同步相关操作的「取消」按钮可用。

**步骤：**
- 1.用普通学校账号（如 tjb 的 operator）登录主界面。2.找「数据备份与恢复」入口。3.依次测：云端同步→看有无取消按钮；暂停同步→同上；清空本地缓存→同上。

**判定：** 有取消按钮且能点（或操作后能关闭/返回）→通过；点了没反应/卡住→失败并截图。

### R4 用户管理：停用后「启用」无效（停用可以但启用不行）

> 负责人：吴晓君

**目的：** 确认用户停用/启用都生效。

**步骤：**
- 1.平台超管 admin-schools.html→选测试学校→用户管理。2.找测试用户：先点「停用」（应成功且该用户登录不了），再点「启用」（应成功且能重新登录）。

**判定：** 停用/启用都生效→通过；停用可以但启用后仍登录不了→失败并截图。

### R5 备份恢复：用户管理→恢复失败

> 负责人：曾水平

**目的：** 确认单校备份能成功恢复。

**步骤：**
- 1.平台超管 admin-schools.html→运维备份。2.选一条单校备份（dmyz 或测试学校）→点「恢复」。3.目标学校选测试学校，确认词输入全大写 RESTORE→执行。4.看恢复过程：准备中→复制中→校验中→完成。

**判定：** 恢复成功、数据回滚正确→通过；报错/失败→失败并完整截图错误提示。⚠️必须用测试学校，绝不用真实在用学校（tjb/zhyz）。

### R6 病原体检测：下载模板打不开（考虑是否取消）

> 负责人：曾水平

**目的：** 确认病原体检测的「下载模板」可用。

**步骤：**
- 1.学校账号登录→进「病原体检测」栏目。2.找「下载模板」按钮→点击。3.看下载的文件能否打开。

**判定：** 能打开（Excel/CSV）→通过；打不开/报错→失败并截图说明是什么格式。备注：如果没必要，实际表现写「建议取消下载模板按钮」。

### R7 界面定制：果蔬农残检测项目无可删除的项目

> 负责人：吴晓君

**目的：** 确认果蔬农残检测项目有删除入口。

**步骤：**
- 1.平台超管 admin-schools.html→界面定制→果蔬农残模块。2.看已添加的检测项目有没有删除按钮。

**判定：** 每个项目有删除按钮且能删→通过；有项目但没有删除按钮→失败并截图。备注：若内置项目不允许删，实际表现说明。

### R8 复检显示：复检填合格仍显示不合格（果蔬/肉蛋/油）

> 负责人：吴翠楠

**目的：** 确认复检填合格后结果显示正确（开发已修复）。

**步骤：**
- 1.学校账号登录 tjb/login.html→果蔬农残，新增一条结果「不合格」保存。2.列表找到该条→点「整改/复检」。3.切「复检录入」标签：复检人员保持默认、复检结果选「合格」、复检说明写一句→提交。4.看结果。5.肉蛋农残、食品油品质同样操作一遍。

**判定：** 列表变成「复检合格」（蓝色徽章）→通过；仍显示不合格→失败并截图。证据：复检弹窗选合格+列表显示复检合格截图。

### R9 界面定制：肉蛋/果蔬新增检测项目后无显示

> 负责人：吴翠楠

**目的：** 确认界面定制新增的检测项目在录入表单可见。

**步骤：**
- 1.平台超管 admin-schools.html→选测试学校→界面定制。2.肉蛋农残→新增检测项目（如「测试项目A」）→保存定制。3.学校账号登录→进肉蛋农残录入表单，看「检测项目」下拉。4.果蔬农残同样操作一遍。

**判定：** 下拉能看到新增项目→通过；看不到→失败并截图。

### R10 界面定制/基本信息：保存修改/保存定制无反应

> 负责人：吴翠楠

**目的：** 确认保存修改/保存定制有反馈。

**步骤：**
- 1.平台超管 admin-schools.html→选测试学校。2.基本信息：改一个字段→点保存修改。3.界面定制：改任意配置→点保存定制。

**判定：** 出现成功提示→通过；无反应→失败，看 Console/Network 记录截图。

### R11 复检数据存储：提交复检后核对 localStorage 的 result 字段是否同步为合格

> 负责人：吴翠楠

**目的：** 确认复检后底层存储的 result 字段同步为「合格」。

**步骤：**
- 1.学校账号登录→果蔬农残，新增一条「不合格」保存。2.按 F12→Application→Local Storage→找到当前站点。3.找到 cache_pesticide（或类似 key）看该记录对象。4.对这条做复检录入选「合格」提交。5.再看 Local Storage 里该记录 result 字段。

**判定：** result 变「合格」且 recheckRecords 最新一条 isPassed:true→通过；仍「不合格」/isPassed false→失败并截图 Local Storage。备注：不会看可跳过填「跳过」。

### R12 界面定制：默认选项回归（新增项目后原有默认选项仍正常）

> 负责人：吴翠楠

**目的：** 确认新增项目后原有默认检测项目仍存在。

**步骤：**
- 1.平台超管界面定制→肉蛋农残，先看新增前有哪些默认检测项目。2.新增一个项目→保存定制。3.学校账号进肉蛋录入表单，看检测项目下拉。4.果蔬同样核对。

**判定：** 原有默认项目仍在+新项目也在→通过；原有默认项目被覆盖不见了→失败并截图。

### R13 登录页：帮助中心跳转无反应/子路径矛盾（V6 帮助中心跳转失败）

> 负责人：曾水平

**目的：** 确认登录页帮助中心跳转正常。

**步骤：**
- 1.打开 tjb/login.html。2.找「帮助中心/需要帮助」入口→点击。3.看跳转。

**判定：** 能打开帮助页（应为 .../tjb/help.html）→通过；无反应/跳错地址/白屏→失败并截图记下实际地址。

### R14 学校登录：非法代码/非法输入负向拦截（V7 登录跳转负向失败）

> 负责人：吴晓君

**目的：** 确认非法学校代码被拦截。

**步骤：**
- 1.打开 tjb/login.html。2.输入错误/不存在的学校代码，点登录。3.也测学校代码空白。

**判定：** 提示「识别不到学校/学校代码不存在」且停留登录页→通过；跳错页/无提示/异常→失败并截图。

### R15 学校列表：回收站「恢复学校」报数据库错误（jsonb 类型不匹配，恢复失败）

> 负责人：吴晓君

**目的：** 确认回收站能恢复学校（开发已定位为代码 bug）。

**步骤：**
- 1.平台超管 admin-schools.html→学校列表→底部/右上「回收站」。2.找一条回收站记录（如 hqyz 横琴一中）→点「恢复」。

**判定：** 恢复成功、学校回到列表→通过；报 visible_types is of type jsonb but expression is of type text / 恢复失败→失败并完整截图错误信息。

### R16 用户管理：创建新用户报「注册失败」（保存按钮被禁用/系统错误）

> 负责人：吴晓君

**目的：** 确认能创建新用户。

**步骤：**
- 1.平台超管 admin-schools.html→选测试学校→用户管理→新增用户。2.填规范账号（用户名 test123、手机号11位、密码≥6位、真实姓名）点保存。

**判定：** 创建成功、列表出现新用户→通过；报「注册失败」或保存按钮灰色点不动→失败并完整截图（含填的字段和报错）。备注：请把填的内容和报错一起截图。

### R17 审计日志：菜单选中但内容区空白无数据

> 负责人：吴晓君

**目的：** 确认审计日志页面正常显示。

**步骤：**
- 1.平台超管 admin-schools.html→左侧「审计日志」菜单。2.看右侧内容区。

**判定：** 显示审计记录列表→通过；空白无任何内容→失败并截图（F12 Console 有无报错一起截）。备注：如果本来没数据，看有没有「暂无数据」提示；没提示直接空白算问题。

### R18 界面定制：打开字段管理 customization 接口返回 500（Network 记录）

> 负责人：吴晓君

**目的：** 确认界面定制接口正常。

**步骤：**
- 1.平台超管→界面定制→打开「字段管理/检测项目」。2.按 F12→Network，操作一下看有无红色（4xx/5xx）请求。

**判定：** 无报错、正常显示→通过；出现 500 错误/页面异常→失败并截图 Network 红色请求+响应内容。备注：不会看 Network 就正常打开字段管理看是否正常，填实际表现。

### R19 界面定制：新增检测项目时「下拉选项」无法添加（暂无选项）

> 负责人：吴翠楠

**目的：** 确认下拉选项能添加。

**步骤：**
- 1.平台超管→界面定制→果蔬/肉蛋农残「新增检测项目」。2.类型选「下拉选择」→在「下拉选项」框输入选项，回车或点+，看能否添加。

**判定：** 能添加多个选项→通过；输了回车没反应/点+没反应→失败并截图操作前后对比。

### R20 界面定制：「保存定制·未保存」按钮文案组合混乱

> 负责人：吴翠楠

**目的：** 确认保存定制按钮文案清晰。

**步骤：**
- 1.平台超管→界面定制→改任意设置，看「保存定制」按钮。

**判定：** 按钮文字清晰（如只有「保存定制」，改动加「未保存」标记且不误导）→通过；文字组合怪异→失败并截图。备注：偏界面优化，小瑕疵可在实际表现说明「建议优化」。

### R21 主登录页：未带 school 参数时报「未识别到有效的学校登录入口」提示不清晰

> 负责人：吴晓君

**目的：** 确认主登录入口提示清晰。

**步骤：**
- 1.浏览器直接打开 index.html（不带 /学校代码/login）。2.看页面。

**判定：** 有正常提示引导用学校专属链接→通过；报错红字没引导/提示不清晰→失败并截图。备注：用户平时走学校专属链接，若认为影响不大可填跳过。

### R22 学校登录：已停用学校的登录链接报「学校不存在或未激活」

> 负责人：吴晓君

**目的：** 确认停用学校的登录提示正常。

**步骤：**
- 1.平台超管把一个测试学校停用。2.打开该校登录链接试登录。

**判定：** 明确提示「该学校已停用/未激活」→通过；提示含糊或直接白屏→失败并截图。

### R23 登录页：学校 Logo 显示默认图标而非学校真实 Logo

> 负责人：吴晓君

**目的：** 确认登录页显示学校真实 Logo。

**步骤：**
- 1.打开某测试学校登录链接 tjb/login.html。2.看页面顶部 Logo。

**判定：** 显示学校真实 Logo（校徽）→通过；显示默认图标→失败并截图。备注：可能该校没上传 Logo，先确认该校是否设置校徽。

### R24 复检录入：弹窗无「复检日期」字段（只有结果/说明/复检人）

> 负责人：吴翠楠

**目的：** 确认复检录入有明确的日期信息。

**步骤：**
- 1.学校账号→果蔬农残→对一条不合格记录点「整改/复检」。2.切「复检录入」标签看表单字段。

**判定：** 有明确的「复检日期」字段→通过；只有结果/说明无日期→失败并截图。备注：若设计是日期自动记录无需手填，填通过并在实际表现注明。

### R25 检测系统左侧菜单：「检测频率与月报」「检测频率与日历」重复出现

> 负责人：曾水平

**目的：** 确认菜单项不重复。

**步骤：**
- 1.学校账号登录→看左侧菜单。2.检查有无重复/含义重叠的菜单项。

**判定：** 菜单清晰不重复→通过；出现「检测频率与月报」「检测频率与日历」重复→失败并截图。

### R26 版本号：检测系统显示 Version 4.0，登录页显示 3.1.0（版本号不一致）

> 负责人：曾水平

**目的：** 确认版本号一致。

**步骤：**
- 1.打开登录页看底部版本号。2.登录进检测系统看版本。

**判定：** 两处版本一致→通过；不一致（登录页 3.1.0 vs 系统 4.0）→失败并两张截图对比。

### R27 检测项目下拉：默认显示占位符「验证设」而非「请选择」

> 负责人：曾水平

**目的：** 确认检测项目下拉默认值正常。

**步骤：**
- 1.学校账号登录→肉蛋农残→看「检测项目」下拉默认显示。2.也检查果蔬/餐具洁净度等其他模块。

**判定：** 默认显示「请选择」或空→通过；显示「验证设」等奇怪文本→失败并截图。

### R28 复检保存后：刷新列表时多个接口报 400 /「记录不存在」（复检记录未正确持久化）

> 负责人：吴翠楠

**目的：** 确认复检提交后无接口报错、记录能保存。

**步骤：**
- 1.学校账号→果蔬农残→对一条不合格记录做复检录入→提交。2.按 F12→Network，看提交后刷新接口。

**判定：** 提交后无 400/404 报错，复检记录正常保存并显示→通过；出现 400 或「记录不存在」→失败并截图 Network 报错+复检记录是否保存。备注：与 R8 相关，重点确认复检记录能正确保存且列表刷新正常。

---
## 附：原始 guide 全文（与 test-report.html 网页完全一致）

**R1** R1 学校列表：无法删除已新增的学校（备注：只能停用不能删除）
```
目的：确认学校列表能否删除新增加的学校。
步骤：1.平台超管登录 admin-schools.html→学校列表。2.找一条测试用的新学校（不要动真实学校）。3.看该行有没有「删除」按钮。
判定：有删除按钮且能删测试学校→通过；只有停用没有删除→失败并截图。备注：如果产品设计就是「只能停用不能删除」，实际表现写「确认是产品设计如此」，结果可填通过。
```

**R2** R2 主界面：返回主界面后无法用任何管理员/学校账号登录
```
目的：确认从管理后台「返回主界面」后能正常登录。
步骤：1.平台超管登录 admin-schools.html。2.操作中点击「返回主界面」。3.看跳转后的页面能否用管理员或学校账号登录。
判定：能正常打开登录页/主界面并登录→通过；打开后登录失败/白屏/无法登录→失败并截图。备注：记录你点的是什么入口、跳到什么地址。
```

**R3** R3 数据同步：云端同步/暂停同步/清空本地缓存后无法点击取消
```
目的：确认数据同步相关操作的「取消」按钮可用。
步骤：1.用普通学校账号（如 tjb 的 operator）登录主界面。2.找「数据备份与恢复」入口。3.依次测：云端同步→看有无取消按钮；暂停同步→同上；清空本地缓存→同上。
判定：有取消按钮且能点（或操作后能关闭/返回）→通过；点了没反应/卡住→失败并截图。
```

**R4** R4 用户管理：停用后「启用」无效（停用可以但启用不行）
```
目的：确认用户停用/启用都生效。
步骤：1.平台超管 admin-schools.html→选测试学校→用户管理。2.找测试用户：先点「停用」（应成功且该用户登录不了），再点「启用」（应成功且能重新登录）。
判定：停用/启用都生效→通过；停用可以但启用后仍登录不了→失败并截图。
```

**R5** R5 备份恢复：用户管理→恢复失败
```
目的：确认单校备份能成功恢复。
步骤：1.平台超管 admin-schools.html→运维备份。2.选一条单校备份（dmyz 或测试学校）→点「恢复」。3.目标学校选测试学校，确认词输入全大写 RESTORE→执行。4.看恢复过程：准备中→复制中→校验中→完成。
判定：恢复成功、数据回滚正确→通过；报错/失败→失败并完整截图错误提示。⚠️必须用测试学校，绝不用真实在用学校（tjb/zhyz）。
```

**R6** R6 病原体检测：下载模板打不开（考虑是否取消）
```
目的：确认病原体检测的「下载模板」可用。
步骤：1.学校账号登录→进「病原体检测」栏目。2.找「下载模板」按钮→点击。3.看下载的文件能否打开。
判定：能打开（Excel/CSV）→通过；打不开/报错→失败并截图说明是什么格式。备注：如果没必要，实际表现写「建议取消下载模板按钮」。
```

**R7** R7 界面定制：果蔬农残检测项目无可删除的项目
```
目的：确认果蔬农残检测项目有删除入口。
步骤：1.平台超管 admin-schools.html→界面定制→果蔬农残模块。2.看已添加的检测项目有没有删除按钮。
判定：每个项目有删除按钮且能删→通过；有项目但没有删除按钮→失败并截图。备注：若内置项目不允许删，实际表现说明。
```

**R8** R8 复检显示：复检填合格仍显示不合格（果蔬/肉蛋/油）
```
目的：确认复检填合格后结果显示正确（开发已修复）。
步骤：1.学校账号登录 tjb/login.html→果蔬农残，新增一条结果「不合格」保存。2.列表找到该条→点「整改/复检」。3.切「复检录入」标签：复检人员保持默认、复检结果选「合格」、复检说明写一句→提交。4.看结果。5.肉蛋农残、食品油品质同样操作一遍。
判定：列表变成「复检合格」（蓝色徽章）→通过；仍显示不合格→失败并截图。证据：复检弹窗选合格+列表显示复检合格截图。
```

**R9** R9 界面定制：肉蛋/果蔬新增检测项目后无显示
```
目的：确认界面定制新增的检测项目在录入表单可见。
步骤：1.平台超管 admin-schools.html→选测试学校→界面定制。2.肉蛋农残→新增检测项目（如「测试项目A」）→保存定制。3.学校账号登录→进肉蛋农残录入表单，看「检测项目」下拉。4.果蔬农残同样操作一遍。
判定：下拉能看到新增项目→通过；看不到→失败并截图。
```

**R10** R10 界面定制/基本信息：保存修改/保存定制无反应
```
目的：确认保存修改/保存定制有反馈。
步骤：1.平台超管 admin-schools.html→选测试学校。2.基本信息：改一个字段→点保存修改。3.界面定制：改任意配置→点保存定制。
判定：出现成功提示→通过；无反应→失败，看 Console/Network 记录截图。
```

**R11** R11 复检数据存储：提交复检后核对 localStorage 的 result 字段是否同步为合格
```
目的：确认复检后底层存储的 result 字段同步为「合格」。
步骤：1.学校账号登录→果蔬农残，新增一条「不合格」保存。2.按 F12→Application→Local Storage→找到当前站点。3.找到 cache_pesticide（或类似 key）看该记录对象。4.对这条做复检录入选「合格」提交。5.再看 Local Storage 里该记录 result 字段。
判定：result 变「合格」且 recheckRecords 最新一条 isPassed:true→通过；仍「不合格」/isPassed false→失败并截图 Local Storage。备注：不会看可跳过填「跳过」。
```

**R12** R12 界面定制：默认选项回归（新增项目后原有默认选项仍正常）
```
目的：确认新增项目后原有默认检测项目仍存在。
步骤：1.平台超管界面定制→肉蛋农残，先看新增前有哪些默认检测项目。2.新增一个项目→保存定制。3.学校账号进肉蛋录入表单，看检测项目下拉。4.果蔬同样核对。
判定：原有默认项目仍在+新项目也在→通过；原有默认项目被覆盖不见了→失败并截图。
```

**R13** R13 登录页：帮助中心跳转无反应/子路径矛盾（V6 帮助中心跳转失败）
```
目的：确认登录页帮助中心跳转正常。
步骤：1.打开 tjb/login.html。2.找「帮助中心/需要帮助」入口→点击。3.看跳转。
判定：能打开帮助页（应为 .../tjb/help.html）→通过；无反应/跳错地址/白屏→失败并截图记下实际地址。
```

**R14** R14 学校登录：非法代码/非法输入负向拦截（V7 登录跳转负向失败）
```
目的：确认非法学校代码被拦截。
步骤：1.打开 tjb/login.html。2.输入错误/不存在的学校代码，点登录。3.也测学校代码空白。
判定：提示「识别不到学校/学校代码不存在」且停留登录页→通过；跳错页/无提示/异常→失败并截图。
```

**R15** R15 学校列表：回收站「恢复学校」报数据库错误（jsonb 类型不匹配，恢复失败）
```
目的：确认回收站能恢复学校（开发已定位为代码 bug）。
步骤：1.平台超管 admin-schools.html→学校列表→底部/右上「回收站」。2.找一条回收站记录（如 hqyz 横琴一中）→点「恢复」。
判定：恢复成功、学校回到列表→通过；报 visible_types is of type jsonb but expression is of type text / 恢复失败→失败并完整截图错误信息。
```

**R16** R16 用户管理：创建新用户报「注册失败」（保存按钮被禁用/系统错误）
```
目的：确认能创建新用户。
步骤：1.平台超管 admin-schools.html→选测试学校→用户管理→新增用户。2.填规范账号（用户名 test123、手机号11位、密码≥6位、真实姓名）点保存。
判定：创建成功、列表出现新用户→通过；报「注册失败」或保存按钮灰色点不动→失败并完整截图（含填的字段和报错）。备注：请把填的内容和报错一起截图。
```

**R17** R17 审计日志：菜单选中但内容区空白无数据
```
目的：确认审计日志页面正常显示。
步骤：1.平台超管 admin-schools.html→左侧「审计日志」菜单。2.看右侧内容区。
判定：显示审计记录列表→通过；空白无任何内容→失败并截图（F12 Console 有无报错一起截）。备注：如果本来没数据，看有没有「暂无数据」提示；没提示直接空白算问题。
```

**R18** R18 界面定制：打开字段管理 customization 接口返回 500（Network 记录）
```
目的：确认界面定制接口正常。
步骤：1.平台超管→界面定制→打开「字段管理/检测项目」。2.按 F12→Network，操作一下看有无红色（4xx/5xx）请求。
判定：无报错、正常显示→通过；出现 500 错误/页面异常→失败并截图 Network 红色请求+响应内容。备注：不会看 Network 就正常打开字段管理看是否正常，填实际表现。
```

**R19** R19 界面定制：新增检测项目时「下拉选项」无法添加（暂无选项）
```
目的：确认下拉选项能添加。
步骤：1.平台超管→界面定制→果蔬/肉蛋农残「新增检测项目」。2.类型选「下拉选择」→在「下拉选项」框输入选项，回车或点+，看能否添加。
判定：能添加多个选项→通过；输了回车没反应/点+没反应→失败并截图操作前后对比。
```

**R20** R20 界面定制：「保存定制·未保存」按钮文案组合混乱
```
目的：确认保存定制按钮文案清晰。
步骤：1.平台超管→界面定制→改任意设置，看「保存定制」按钮。
判定：按钮文字清晰（如只有「保存定制」，改动加「未保存」标记且不误导）→通过；文字组合怪异→失败并截图。备注：偏界面优化，小瑕疵可在实际表现说明「建议优化」。
```

**R21** R21 主登录页：未带 school 参数时报「未识别到有效的学校登录入口」提示不清晰
```
目的：确认主登录入口提示清晰。
步骤：1.浏览器直接打开 index.html（不带 /学校代码/login）。2.看页面。
判定：有正常提示引导用学校专属链接→通过；报错红字没引导/提示不清晰→失败并截图。备注：用户平时走学校专属链接，若认为影响不大可填跳过。
```

**R22** R22 学校登录：已停用学校的登录链接报「学校不存在或未激活」
```
目的：确认停用学校的登录提示正常。
步骤：1.平台超管把一个测试学校停用。2.打开该校登录链接试登录。
判定：明确提示「该学校已停用/未激活」→通过；提示含糊或直接白屏→失败并截图。
```

**R23** R23 登录页：学校 Logo 显示默认图标而非学校真实 Logo
```
目的：确认登录页显示学校真实 Logo。
步骤：1.打开某测试学校登录链接 tjb/login.html。2.看页面顶部 Logo。
判定：显示学校真实 Logo（校徽）→通过；显示默认图标→失败并截图。备注：可能该校没上传 Logo，先确认该校是否设置校徽。
```

**R24** R24 复检录入：弹窗无「复检日期」字段（只有结果/说明/复检人）
```
目的：确认复检录入有明确的日期信息。
步骤：1.学校账号→果蔬农残→对一条不合格记录点「整改/复检」。2.切「复检录入」标签看表单字段。
判定：有明确的「复检日期」字段→通过；只有结果/说明无日期→失败并截图。备注：若设计是日期自动记录无需手填，填通过并在实际表现注明。
```

**R25** R25 检测系统左侧菜单：「检测频率与月报」「检测频率与日历」重复出现
```
目的：确认菜单项不重复。
步骤：1.学校账号登录→看左侧菜单。2.检查有无重复/含义重叠的菜单项。
判定：菜单清晰不重复→通过；出现「检测频率与月报」「检测频率与日历」重复→失败并截图。
```

**R26** R26 版本号：检测系统显示 Version 4.0，登录页显示 3.1.0（版本号不一致）
```
目的：确认版本号一致。
步骤：1.打开登录页看底部版本号。2.登录进检测系统看版本。
判定：两处版本一致→通过；不一致（登录页 3.1.0 vs 系统 4.0）→失败并两张截图对比。
```

**R27** R27 检测项目下拉：默认显示占位符「验证设」而非「请选择」
```
目的：确认检测项目下拉默认值正常。
步骤：1.学校账号登录→肉蛋农残→看「检测项目」下拉默认显示。2.也检查果蔬/餐具洁净度等其他模块。
判定：默认显示「请选择」或空→通过；显示「验证设」等奇怪文本→失败并截图。
```

**R28** R28 复检保存后：刷新列表时多个接口报 400 /「记录不存在」（复检记录未正确持久化）
```
目的：确认复检提交后无接口报错、记录能保存。
步骤：1.学校账号→果蔬农残→对一条不合格记录做复检录入→提交。2.按 F12→Network，看提交后刷新接口。
判定：提交后无 400/404 报错，复检记录正常保存并显示→通过；出现 400 或「记录不存在」→失败并截图 Network 报错+复检记录是否保存。备注：与 R8 相关，重点确认复检记录能正确保存且列表刷新正常。
```
