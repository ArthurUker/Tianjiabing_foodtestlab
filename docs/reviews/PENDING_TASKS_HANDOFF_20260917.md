# 挂起任务交接说明（RDPMS / foodSentinel 开放接口）

> 生成时间：2026-09-17（北京时间）｜用途：交付外部审阅（GPT）分析
> 本文**自包含**：先读 §0 背景即可理解全部上下文，无需访问代码库。

---

## ✅ 交接文档更新（2026-09-17 复核轮追加；下方 §0 起为原始交接内容，保留原文）

**本轮（复核对 + HTTP 层）已完成，未提交：**
- 新增 HTTP 链路集成测试 `backend/tests/http/openapi-http.integration.test.mjs`（**8/8**，真实 Express + 真实 API-Key 中间件 + 本地随机端口）。
- 隔离门禁加固：`backend/tests/_isolation.mjs` 统一把 `process.env.DATABASE_URL` 重定向到隔离库（此前靠各用例自觉）。
- **修正一处对账契约表述错误**（N1）：无授权日期范围时 `/test-records` 含日期缺失/非法记录 → 对账恒等式是 `manifest.total = scope_total + excluded_total`（文档与 HTTP 测试同步）。
- TPM 表述降级为"待核实"：新增机器可读 `unit_source:'platform_label'` / `unit_verified:false`，删除"等价于 %/勿再 ×100"断言式表述（**运行行为变化：响应新增字段**）。
- 交付物：`OPENAPI_F1_F8_RECHECK_AND_HTTP_TESTS_20260917.md`（复核报告）、`TPM_UNIT_VERIFICATION_CHECKLIST_20260917.md`、`F9_DUAL_METRIC_CONTRACT_DRAFT_20260917.md`、`THIRD_PARTY_RESYNC_ACCEPTANCE_20260917.md`、`APP_PROTOCOL_CHECKLIST_20260917.md`。
- 测试分层：纯函数 95（92 pass / 3 skip＝未设隔离库变量）、隔离库 35、HTTP 8、生产只读脚本 12 —— **F1–F5/F7/F8 复核通过；F6 部分通过（缺父版本→当前版本端到端升级测试）**。

**状态修正（相对下方原文）：**
- 任务 4（TPM）：**表述已改**，核实资料仍缺（见 TPM 清单）。
- 任务 5（F9）：**契约草案已产出**，仍待业务确认。
- 任务 6（`c5489e1` 未推送）：**仍未推送**；本轮又新增了未提交改动（见上）。
- 任务 5（生产写入实测）：**仍未做**（本轮同样受"不得写生产"限制）；新增"内部路由缺 HTTP 层测试"缺口（原因=避免伪造 `req.user`）。

---

## §0 背景速览（审阅者必读）

| 项 | 值 |
|---|---|
| 系统 | RDPMS / foodSentinel —— 校园食品安全检测数据平台（多学校 = 多 PostgreSQL schema，`school_<code>`） |
| 仓库 | `git@github.com:ArthurUker/Tianjiabing_foodtestlab.git`（分支 `Product_tencent_CVM`） |
| 本批相关提交 | `d090b77`（/stats 日期 500 修复）→ `a877901`（P0/P1 正确性 + 外部审阅 F1–F9 处置）→ `d87d445`（发布报告 §J）→ `c5489e1`（只读核验脚本修正，**未推送**） |
| 线上状态 | 生产已发布 `d87d445`（2026-09-17 12:41 构建 dist + 重启服务）；**唯一未上线物是 `c5489e1`（仅测试脚本，不影响运行）** |
| 测试基线 | 纯函数/契约 `node --test tests/` = 95 项（92 pass / 0 fail / 3 skipped＝需隔离库环境变量）；隔离库集成 35/35；生产只读行为核验 12/12 |
| 开放接口 | 对外只读 8 端点（ping/profile/schools/dict/samples/test-records/sync/manifest/stats）+ 超管 9 端点（clients/credentials/grants/preview/dict/samples/package） |
| 唯一真实对接方 | 朴食科技（`test` 校 active；tjb/zhsy/zhyz disabled） |
| 已确立的关键不变量 | ① 未提交的数据不得被意外删除；② 未完成的同步不得被标记为完成；③ 无权访问的数据不得由统计推知；④ 统计集合互斥且数学自洽；⑤ 脏日期既不有效也不触发 500；⑥ 换写入入口并发控制不得失效；⑦ 未知业务状态不得自动升为"合格" |
| 本轮已闭环 | 上述 7 条不变量全部有反例回归；外部审阅 F1–F8 已修，F9 仅做契约声明 |

下面每项任务均给出 8 个字段：名称／当前状态／已完成进展／未完成部分／卡住原因／依赖与阻塞项／最近更新时间／需进一步分析或决策的重点。

---

## 任务 1：F9 指标口径决策 —— 合格率按「初检」还是「最终结论」

- **当前状态**：待业务决策（代码侧仅做契约声明，未改语义）
- **已完成进展**：
  - `/stats` 新增 `metric_basis='initial_conclusion'` 与 `metric_basis_note`，明确"明细 `conclusion=pass` 却未计入 `pass_count` 是**预期差异**（初检不合格→复检合格）"；
  - 文档 `docs/OPEN_API_INTEGRATION.md` §5.7 同步声明；
  - 统计 SQL 仍按**初检**判定（与员工端看板同口径），未擅自改动 v1 指标；
  - 外部审阅 F9 提出的"统计与明细结论分叉"已如实归类为**既有问题**并声明。
- **尚未完成的部分**：是否提供按**最终结论**统计的口径（评审建议以**新字段**如 `pass_rate_final` 提供，而非改旧字段含义）；若决定新增，需要：字段设计 → SQL → 文档 → 契约测试 → 发版。
- **卡住的原因**：属**业务口径决策**（指标语义），非技术问题；改变会直接影响对接方已建立的模型与历史对账。
- **相关依赖 / 阻塞项**：业务方（校方食品安全管理方）与对接方（朴食科技）确认；无技术阻塞。
- **最近一次更新**：2026-09-17（随 `a877901` 上线）
- **需要进一步分析或决策的重点**：
  1. 对接方对账时能否接受"初检合格率 + `excluded` + `metric_basis` 声明"这一组合？
  2. 若新增最终结论口径：命名（`pass_rate_final` / `final_pass_rate`）、是否同时提供 `by_type` 维度、是否需要历史回算（历史记录结论是**录入时冻结**的，不能重算）。
  3. 两条口径并存时如何避免"两个合格率"被误用（建议在响应里互相声明、在接入包中给出对比示例）。

---

## 任务 2：TPM（食用油极性组分）单位与阈值的**外部权威规格**缺失

- **当前状态**：待外部资料；本轮**刻意未改**任何单位或阈值
- **已完成进展**：
  - 仓库内证据链已固化：前端 `frontend/js/modules/GenericTest.js:652` 以 `g/100g` 展示、`getTpmLevel()` 用 `≤0.13 合格`（`≤0.25 警戒`，`>0.25 不合格`）；
  - 字段字典写明 `result.tpmValue` 为**字符串**、`0.06 = 0.06 g/100g（等价 0.06%）`、**勿再 ×100**、实测范围 0.06~0.20；
  - 合成样例 pass/fail 已与阈值自洽（`0.06` vs `0.31`）。
- **尚未完成的部分**：设备原始输出协议（量纲：质量分数 vs 体积分数；是否已做换算）、0.13/0.25 阈值的标准出处（国标/团标/企业内控）。
- **卡住的原因**：设备协议文档不在仓库、也未被提供；**不得凭"前后端一致"当成外部正确性证据**。
- **相关依赖 / 阻塞项**：设备型号与厂商协议文档；业务方提供。
- **最近一次更新**：2026-09-17（字典措辞与样例在 `a877901` 中定稿）
- **需要进一步分析或决策的重点**：
  1. 若设备输出与 `g/100g` 不一致，**历史数据是否需换算**（涉及阈值判定与历史结论一致性，属高风险变更）；
  2. 阈值出处若与现行不同，是否有"历史结论不重算、仅新数据生效"的明确约定；
  3. 是否需要为 `tpmValue` 增加"原始值 + 单位"双字段以彻底消除歧义（属契约变更，需评估对接方影响）。

---

## 任务 3：App 侧是否启用 `expected_version`（乐观锁 CAS）

- **当前状态**：服务端已具备并明确语义；App 侧是否使用**待定**
- **已完成进展**：
  - `POST /api/sync/records`、`/api/sync/batch`：带 `expected_version`（或 `version`）→ **原子 CAS**，冲突返回 `409 VERSION_CONFLICT`（批量为 `errors[]` 中同名 code）；不带 → **明确 LWW** 并打印日志；
  - 所有写入口 `version` **原子 +1**（含此前不递增的 legacy `PUT /api/test-records/:id`）；
  - `bulk-upsert` 的 `expected_updated_at` 改为**原子 CAS**（条件含 `updated_at`）；
  - 语义已写入 `README §4.4` 表格与代码注释（"不要把 sync 端点的 version 当锁"）。
- **尚未完成的部分**：App 离线队列是否保存并回传 `version`；是否需要冲突交互（提示用户"记录已被他人修改"）；App 兼容性**完全未验证**（源码不在本仓）。
- **卡住的原因**：App 源码/协议不在本次可访问范围；无法验证其载荷形态与队列行为。
- **相关依赖 / 阻塞项**：App 团队；若启用 CAS 还需评估离线场景下的 409 频率。
- **最近一次更新**：2026-09-17（`a877901`）
- **需要进一步分析或决策的重点**：
  1. 离线优先场景中，CAS 是否会造成高频冲突（例如同一记录被多人/多设备编辑）；
  2. 是否需要折中方案："默认 LWW + 冲突留痕（审计）"，或"仅对 manager 角色启用 CAS"；
  3. App 若不回传版本号，是否需要服务端在响应里回传 `applied_version` 以便其后续启用 CAS（当前响应已含记录对象，含 version）。

---

## 任务 4：超管界面「接入说明」真机验证（含窄屏）

- **当前状态**：已发布上线，**等待人工点击确认**（我无法使用你的登录态）
- **已完成进展**：
  - 界面四块：开放范围摘要 / 字段字典 / 样例预览 / 接入包下载（含**空态**与**错误态**）；
  - 字典表新增：**「下发」列**（`emitted:false` 行灰显 + "不下发"角标）、**枚举取值**（如 `enum（取值：pass / fail / warning / unknown）`）、**数组元素子结构**、**条件字段**提示；
  - 线上前端产物已核验 = 本地 dist（`openApiView.js = 2e5e24d7…`）。
- **尚未完成的部分**：真实登录态下的四块渲染确认、窄屏（移动端）可读性、接入包下载文件内容抽查（枚举/不下发标记/快照声明）。
- **卡住的原因**：需要你的浏览器会话（自动化无法登录）。
- **相关依赖 / 阻塞项**：你的登录态；无代码阻塞。
- **最近一次更新**：2026-09-17 12:41（发布上线）
- **需要进一步分析或决策的重点**：
  1. 字典表在窄屏是否需要横向滚动或折叠（当前 8 列，宽屏可读）；
  2. 接入包下载的 **Markdown** 是否需要同时提供 PDF/HTML（当前仅 `.md`）；
  3. 是否需要"未保存草稿"提示在授权改版后再次校验（当前已有未保存提示）。

---

## 任务 5：P0-1 / F1 的**生产库写入**真机验证（刻意未做）

- **当前状态**：以**等价证据**替代，未做生产写入实测
- **已完成进展**：
  - 隔离库（独立数据库 + 独立 schema）对**真实路由末端 handler + 真实 Prisma**做了 12 项反例回归：仅上下文提交不清空、扁平业务字段生效、`{}`/缺省/`null` 不改动、非法结构 400、兄弟字段保留、`replace` 与 `merge` 差异、CAS 409、LWW 递增、批量一致、bulk CAS；
  - 生产**只读**验证 12/12（分页/时间/统计口径自洽）；
  - 等价证据：线上部署文件与通过上述回归的提交 `d87d445` **逐字节一致**（工作区 clean）。
- **尚未完成的部分**：在生产库上创建**临时记录**验证写入路径（例如"只提交 `result_data:{canteen:'B'}` → 原结果保留"）。
- **卡住的原因**：需要写生产数据；评估为"风险/收益不划算"，故未做（此前已明确告知你，可选择补做）。
- **相关依赖 / 阻塞项**：你的授权；若做，方式是"临时记录 + 验完删除 + 复核记录数恢复"。
- **最近一次更新**：2026-09-17（发版后）
- **需要进一步分析或决策的重点**：是否接受"同源代码 + 隔离库反例"作为等价证据；若必须生产实测，建议固定为一个可回滚的临时记录并留存前后快照。

---

## 任务 6：`c5489e1` 尚未推送到远端

- **当前状态**：本地已提交，远端仍为 `d87d445`
- **已完成进展**：提交内容 = 只读核验脚本改为引用同源日期谓词 + 互斥桶/恒等式断言；并已在**生产库只读**复验 12/12。
- **尚未完成的部分**：`git push origin Product_tencent_CVM`（一条命令）。
- **卡住的原因**：等你的确认（改动仅测试脚本，不影响线上运行）。
- **相关依赖 / 阻塞项**：无。
- **最近一次更新**：2026-09-17
- **需要进一步分析或决策的重点**：是否与后续改动合并推送（减少推送次数）。

---

## 任务 7：Git 本地跟踪引用陈旧 / `@{upstream}` 不可解析

- **当前状态**：已诊断，**未修**（安全约定禁止我改 git config）
- **已完成进展**：定位到根因 —— `remote.origin.fetch` 只配置了 `main`（`+refs/heads/main:refs/remotes/origin/main`），因此 `branch.Product_tencent_CVM.merge` 无法映射到远端跟踪引用；`git status -sb`、`@{upstream}` 报 `not stored as a remote-tracking branch`。判定远端状态一律改用 `git ls-remote`。
- **尚未完成的部分**：若需恢复 `@{upstream}` 能力，需追加一行 refspec。
- **卡住的原因**：属 git config 变更，需你明确同意。
- **相关依赖 / 阻塞项**：你的一句话确认。
- **最近一次更新**：2026-09-17
- **需要进一步分析或决策的重点**：是否有必要（当前显式 `git push origin <branch>` 工作正常）；若改，命令与回滚方式已在会话中给出。

---

## 任务 8：对接方"首拉全量重投影"的沟通与观察

- **当前状态**：机制已生效（预期行为），沟通未做
- **已完成进展**：`projection_fingerprint` 现包含**投影实现修订号** + **学校配置指纹**，并传导到 `manifest digest`；因此本次发布后，朴食科技下次同步会看到指纹/摘要变化，触发**一次全量重投影**（幂等，无数据风险）；该行为已写入文档与报告。
- **尚未完成的部分**：是否提前知会对接方；是否需要观察其同步日志/对账结果。
- **卡住的原因**：需人工沟通（我无对方渠道）。
- **相关依赖 / 阻塞项**：对接方（朴食科技）。
- **最近一次更新**：2026-09-17（随发布生效）
- **需要进一步分析或决策的重点**：若对方有"清单变化告警"，需要给他们一句解释话术（"投影修订号变化 → 一次全量重投影，属预期；本地按替换式写入即可"）。

---

## 任务 9：历史文档口径指针（低风险小改，未排期）

- **当前状态**：已识别，未修
- **已完成进展**：`docs/reviews/OPEN_API_STATS_AND_CONTRACT_FIX_20260916.md` 中仍描述**已被删除**的旧字段 `out_of_range_total`（语义含糊、且会暴露授权外数量）。
- **尚未完成的部分**：在该报告顶部加一行"字段定义已被 `RDPMS_OPENAPI_P0P1_CORRECTNESS_FIX_20260917.md` 取代"的指针；或在旧文内标注删除原因。
- **卡住的原因**：优先级低，未排期。
- **相关依赖 / 阻塞项**：无。
- **最近一次更新**：2026-09-17（识别）
- **需要进一步分析或决策的重点**：无技术争议；仅需避免读者按旧字段开发。

---

## 任务 10：历史导入脚本仍写"上下文同义副本"（刻意保留）

- **当前状态**：文档已记录现状，代码**刻意未改**
- **已完成进展**：`backend/scripts/README.md` 增加"存量导入脚本现状"表（执行模式/与现行口径差异/风险）；平台自 2026-09-16 起新记录不再产生副本，字段字典把副本标为"历史同义副本（以顶层为准）"。
- **尚未完成的部分**：是否把 `import-tjb-backup.mjs`、`import-zhyz-backup.mjs` 对齐现行口径（去掉副本）。
- **卡住的原因**：改动会改变脚本产出的内容 → 影响其**内容哈希/去重标识**，可能导致重复导入；且脚本已执行完毕。
- **相关依赖 / 阻塞项**：是否存在"重新导入/恢复备份"的真实需求。
- **最近一次更新**：2026-09-17
- **需要进一步分析或决策的重点**：若将来重导，去重策略（按 `record_code` 内容哈希 vs 业务键）与副本残留在对外口径下的可接受性。

---

## 附：优先级建议与"需要人拍板"的清单

| 优先级 | 任务 | 需要谁 |
|---|---|---|
| 高（影响对外指标可信度） | 任务 1（F9 口径） | 业务方 + 对接方 |
| 高（影响业务判定正确性） | 任务 2（TPM 规格） | 业务方 + 设备方 |
| 中（影响客户端行为） | 任务 3（App CAS） | App 团队 |
| 中（体验确认） | 任务 4（界面真机） | 你 |
| 低（工程整洁） | 任务 6（推送）、任务 7（refspec）、任务 9（文档指针） | 你 |
| 低（风险控制） | 任务 5（生产写入实测）、任务 10（导入脚本口径） | 你 + 业务判断 |
| 沟通类 | 任务 8（对接方知会） | 你 |

**给审阅者（GPT）的建议追问方向**：
1. 任务 1 的两条口径并存是否会产生新的对账歧义？是否有比"新增字段"更好的方案？
2. 任务 2 在缺少设备协议的前提下，是否存在更稳妥的过渡策略（如双字段/单位标注/仅新数据生效）？
3. 任务 3 的 CAS 与离线优先之间的取舍是否有行业惯例可参照？
4. 任务 5 的"同源代码 + 隔离库反例"是否足以作为生产正确性证据？还缺哪一类证据？
