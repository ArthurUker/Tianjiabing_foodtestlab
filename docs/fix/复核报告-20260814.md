# R&D PMS 测试报告二次深度审阅与修复方案交叉验证

> **复核日期**：2026-08-14
> **复核对象**：`docs/fix/待修复问题深度分析-20260814.md`
> **数据源**：`docs/test-results/latest/snapshot.json`（md5: `843f27f3...`，mtime: 2026-08-14 13:32）

---

## 一、Gate-1：数据源唯一性核验

| 数据源 | 路径 | mtime | md5 | 采信 |
|---|---|---|---|---|
| **权威源 A** | `docs/test-results/latest/index.html` | 2026-08-14 13:32 | `843f27f3...` | ✅ |
| **权威源 B** | `docs/test-results/latest/snapshot.json` | 2026-08-14 13:32 | (同上) | ✅ |
| **权威源 C** | `docs/test-results/latest/REPORT.md` | 2026-08-14 13:32 | (同上) | ⚠️ 汇总数字有偏差，以 snapshot 原始字段为准 |
| 线上镜像 | `http://111.231.166.161:8080/docs/test-results/latest/index.html` | — | `843f27f3...` | ✅ 与本地一致 |
| **旧版（不采信）** | 根目录 `test-report.html` | 2026-08-14 11:41 | `a2f8cf28...` | ❌ md5 不同，时间早 2h |

**结论**：本轮分析**仅以 `docs/test-results/latest/` 三件套为准**。根目录 `test-report.html` 为旧版快照，不采信。

---

## 二、Gate-2：状态矩阵全量核对

> 对 snapshot.json 中全部 75 条记录逐条读取 `result` + `closed` + `closed_by` + `closed_at` 四字段。

### 状态矩阵表

| case_id | result | closed | 收口人 | closed_at | 纳入本轮 |
|---|---|---|---|---|---|
| Q1-果蔬阶段A | failed | ✅ 收口 | renkang | 2026-08-14T02:50:23Z | **否** |
| Q1-果蔬阶段B | passed | ✅ 收口 | renkang | 2026-08-14T02:50:31Z | 否(非failed) |
| Q1-果蔬阶段C | passed | ✅ 收口 | renkang | 2026-08-14T02:52:40Z | 否(非failed) |
| Q1-肉蛋 | failed | ✅ 收口 | renkang | 2026-08-14T02:54:33Z | **否** |
| Q1-油 | passed | ✅ 收口 | renkang | 2026-08-14T02:55:01Z | 否(非failed) |
| V8-基本信息 | passed | ✅ 收口 | renkang | 2026-08-14T03:10:48Z | 否(非failed) |
| V8-界面定制 | pending | ✅ 收口 | renkang | 2026-08-14T03:10:49Z | 否(非failed) |
| V1-肉蛋 | pending | ✅ 收口 | renkang | 2026-08-14T03:10:55Z | 否(非failed) |
| V1-果蔬 | failed | ✅ 收口 | renkang | 2026-08-14T03:11:09Z | **否** |
| 回归V2 | passed | ✅ 收口 | renkang | 2026-08-14T03:11:47Z | 否(非failed) |
| **V6-帮助跳转** | **failed** | open | — | — | **✅ 是** |
| **V7-负向** | **failed** | open | — | — | **✅ 是** |
| 回归V3 | passed | open | — | — | 否(通过) |
| 回归V4 | passed | open | — | — | 否(通过) |
| 回归V5 | passed | open | — | — | 否(通过) |
| **回归V6地址** | **failed** | open | — | — | **✅ 是** |
| 回归V7正向 | pending | open | — | — | 否(pending) |
| B1~B6 | passed | open | — | — | 否(通过) |
| B7-负向 | passed | open | — | — | 否(通过) |
| B7-正向 | passed | open | — | — | 否(通过) |
| **B7-旧schema** | **failed** | open | — | — | **✅ 是** |
| B8 | skipped | open | — | — | 否(跳过) |
| **B9** | **failed** | open | — | — | **✅ 是** |
| **R01** | **failed** | open | — | — | **✅ 是** |
| R02~R12,R14,R16~R18 | passed | open | — | — | 否(通过) |
| **R03** | **failed** | open | — | — | **✅ 是** |
| **R06** | **failed** | open | — | — | **✅ 是** |
| **R07** | **failed** | open | — | — | **✅ 是** |
| **R13** | **failed** | open | — | — | **✅ 是** |
| **R15** | **failed** | open | — | — | **✅ 是** |
| **R19** | **failed** | open | — | — | **✅ 是** (⚠️ 上版遗漏) |
| R20~R28 | pending | open | — | — | 否(pending) |
| P01~P14 | pending | open | — | — | 否(pending) |
| **NEW-170443** | **failed** | open | — | — | **✅ 是** |
| **NEW-140845** | **failed** | open | — | — | **✅ 是** |
| **NEW-140219** | **failed** | open | — | — | **✅ 是** |
| **NEW-170648** | **failed** | open | — | — | **✅ 是** |
| **NEW-170647** | **failed** | open | — | — | **✅ 是** |

### 统计摘要

| 维度 | 数量 |
|---|---|
| 总用例数 | **75** |
| result=passed | 28 |
| result=failed | **20** |
| result=pending | 26 |
| result=skipped | 1 |
| **closed=true（收口）** | **10** |
| **failed 且 !closed（待修复失败项）** | **17** |
| 已收口的 failed 项 | 3（Q1-果蔬阶段A / Q1-肉蛋 / V1-果蔬） |

---

## 三、Gate-3：计数勾稽

### REPORT.md 顶部汇总 vs snapshot.json 实际值

| 指标 | REPORT.md 顶部 | snapshot.json 实际 | 差异原因 |
|---|---|---|---|
| 通过 | 23 | **28** | REPORT.md 可能仅统计"未收口的 passed"或存在统计脚本 bug |
| 失败 | 17 | **20** | 同上；且 REPORT.md 的 17 与本表"failed && !closed = 17"数值巧合一致但口径不同 |
| 跳过 | 1 | 1 | 一致 ✅ |
| 待测 | 24 | 26 | 差 2（可能因 pending 中含 2 个已收口项被排除） |
| 收口 | 10 | 10 | 一致 ✅ |
| **总数** | **75** | **75** | 一致 ✅ |

**勾稽结论**：总数 75 一致，但"通过/失败/待测"三列的 REPORT.md 汇总值与 snapshot 原始计数存在 **+5/-3/-2** 的偏差。偏差来源推测为 report-sync 生成脚本的统计口径（可能对已收口项做了特殊处理）。**本轮以 snapshot.json 原始字段值为准**。

**待修复失败项 = 17 个**（上一版文档声称 16 个，**漏了 R19**）。

---

## 四、逐条 FIX 复核表（17 项 failed-open）

### FIX-03：V6 / 回归V6地址 / R13 — 帮助中心跳转失败（3 合 1）

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `V6-帮助跳转/1786586900438_cf468e4b.png` + `回归V6地址/1786609703018_9deb7326.png` + `R13/1786609958504_3b8b5719.png` 均显示控制台 GET `/<code>/help.html` 返回 404/409。与 REPORT.md 描述一致 ✅ |
| **根因置信度** | **高（可验证闭环）** |
| **根因代码定位** | `login.html:281` `<a href="./help.html">` → 相对路径解析为 `/<code>/help.html` → Caddy `deploy.sh:694-698` 仅 `@schoolLogin` rewrite login 页 → 无 help.html 子路径兜底 → `try_files` 找不到 `dist/<code>/help.html` → fallback 到 `dist/index.html` |
| **DB/部署验证** | `dist/` 下确认无任何 `<code>/help.html` 文件；`help.html` 仅存在于根级 `dist/help.html`。Caddy 配置全文搜索确认仅有 1 条 rewrite 规则 |
| **影响面** | 全部子路径部署的学校登录页均受影响（约 N 所学校）；根路径 `/login.html` 不受影响。`help.html:19` 返回链接同为相对路径但因 `@schoolLogin` rewrite 兜底而碰巧可用 |
| **修复方案** | **方案 A（推荐）**：`login.html:281` 改为 `<a href="/help.html?school={currentCode}">`，`help.html` 读 `?school=` 参数保留上下文；返回链接改绝对路径。**方案 B**：deploy.sh 新增 `@schoolHelp` path_regexp rewrite 到 `/help.html` |
| **回滚方案** | git revert 即可回滚到相对路径行为 |
| **验证用例** | ① 浏览器打开 `/<任意code>/login.html` → 点"需要帮助？" → 应跳转到 `/help.html?school=<code>` 并正常渲染 ② 根路径 `/login.html` 点帮助仍正常 ③ help.html "返回登录"链接回到对应学校登录页 |

---

### FIX-04：V7 — 超管登录非法输入无红框提示

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `V7-负向/1786588047906_063a5f8d.png` 显示输入框空白无红框。**但截图证据存疑**——截图拍摄的是页面初始状态，未展示用户实际输入非法字符后的瞬间（红框仅持续 1500ms 后自动消失） |
| **根因置信度** | **低（代码核查与工单描述矛盾）** |
| **根因代码定位** | `super-admin-login.html:202-217` 校验逻辑完整：空值触发 `ring-2 ring-red-400`（205行），非法字符正则触发同类红框（212行）。Tailwind 构建方式为预编译 CLI（`package.json:25`），产物 `css/tailwind.css` 已确认包含 `.ring-red-400{...}` 定义 |
| **代码原文** | ```js
// 第211-216行（完整原文）
if (!/^[a-z0-9-]+$/.test(c)) {
    schoolCodeInput.classList.add('ring-2', 'ring-red-400');
    schoolCodeInput.value = '';
    schoolCodeInput.placeholder = '学校代码仅含小写字母/数字/连字符';
    setTimeout(() => schoolCodeInput.classList.remove('ring-2', 'ring-red-400'), 1800);
    return;
}
``` |
| **差异分析** | 代码链路完整闭合（JS加class → CSS有定义 → 页面引入CSS）。若现场仍无红框，可能原因：(a) 用户测试的是**账号/密码框**（那两个框非法输入走 `showError()` 显示横幅而非红框）；(b) 学校代码输入框默认藏在折叠区 `#schoolCodeBox`（`:87` hidden），用户未展开就断言无红框；(c) 红框 1500ms 自动消失，截图时机错过 |
| **修复方案** | **暂不排期修复**。建议先复现确认：在 `#schoolCodeBox` 展开状态下输入中文/特殊字符，观察是否出现红框。若确认为账号/密码框的问题，则需给那两个框补充红框逻辑 |
| **验证用例** | 打开 super-admin-login.html → 展开"使用学校代码进入"区域 → 输入 "中文测试!" → 应看到红色边框 + placeholder 变更 → 1.5s 后自动消失 |

---

### FIX-06：B7 — 旧 schema 未清理

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `B7-旧schema/1786600860096_c9e60a85.png` 显示 psql 查询能找到 `_old_<timestamp>` schema，手动 DROP 因 Windows PowerShell 语法问题失败。与描述一致 ✅ |
| **根因置信度** | **高（代码确认）** |
| **根因代码定位** | `backend/lib/restoreService.js:149-155`：`if (process.env.RESTORE_DROP_OLD !== 'keep')` 分支体内**仅有 `log()` 调用，无任何 `runPsql(DROP SCHEMA)` 执行**。无论环境变量取何值，成功路径永不 DROP |
| **代码原文** | ```js
// 第149-155行（完整原文）
if (process.env.RESTORE_DROP_OLD !== 'keep') {
    log(`${TAG} 旧 schema 保留: ${oldSchema}（确认无误后手动 DROP SCHEMA "${oldSchema}" CASCADE 清理）`)
    // ← 只有log，没有DROP执行！if分支体与else分支（不存在）行为完全相同
}
``` |
| **附带发现** | ① 注释自相矛盾（`:152` 声称"默认 keep 更安全"，但条件是 `!== 'keep'` 即"非 keep 则删除"）② `RESTORE_DROP_OLD` 是无效配置项（设任何值不改变行为）③ 唯一的 DROP 在**失败路径**（`:173`），删的是临时 restoreSchema 而非 oldSchema ④ 无定时清理任务 |
| **修复方案** | 在 `:150` 分支内增加：`await runPsql({ command: \`DROP SCHEMA IF EXISTS "${oldSchema}" CASCADE\` }).catch((e) => log(\`清理旧schema失败(非致命):\${e.message}\`))` |
| **回滚方案** | 设环境变量 `RESTORE_DROP_OLD=keep` 即恢复当前"只保留不删"行为 |
| **验证用例** | 执行一次影子恢复 → 恢复完成后检查 pg_namespace → 确认 `_old_*` schema 已自动消失（或设 RESTORE_DROP_OLD=keep 时保留） |

---

### FIX-07：B9 — SSH侧检查（定时任务/文件权限/告警）

| 维度 | 结论 |
|---|---|
| **现象复核** | 报告 detail 为 "SSH侧检查定时任务/文件权限/失败告警机制——检查失败"。**无截图证据**（evidence relPath 为 undefined） |
| **根因置信度** | **低（上一版分析有误，文件实际存在）** |
| **修正说明** | 上一版文档称 `scripts/003_backup-now.mjs` 不存在。**经本次全量搜索确认该文件实际存在于仓库**（`scripts/003_backup-now.mjs`，大小 4175 字节）。上一版的搜索命令可能遗漏了 `.mjs` 后缀 |
| **实际状态** | systemd timer ExecStart 指向的脚本**存在**。B9 失败的真实原因需进一步排查：(a) 服务是否真正 enable+start？(b) TENCENT_SECRET_ID/KEY 或 BACKUP_MASTER_KEY 是否已配置？(c) 脚本执行时的具体报错？ |
| **修复方案** | **需补证后再定**。建议 SSH 登录目标服务器执行：`systemctl status food-backup.timer && journalctl -u food-backup.service -n 50` 查看真实失败原因 |
| **验证用例** | 待补证后补充 |

---

### FIX-08：R01 / R15 — 学校删除/恢复报 jsonb 类型错误

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `R01/1786604522886_4827a38c.png` + `R15/1786611604909_aef7d255.png` 均显示 PostgreSQL 错误 `42804: column "visible_types" is of type jsonb but expression is of type text`。与 REPORT.md 完全一致 ✅ |
| **根因置信度** | **高（100% 可验证，代码+DDL 双重确认）** |
| **根因代码定位** | `backend/server.js:1146-1151`（restore INSERT）：12 个 jsonb 列使用 `${placeholders}`（`$3..$14`）**无 `::jsonb` cast**。传入值为 JSON.parse 后对象的字符串属性（原 jsonb 列经 `$queryRawUnsafe` 返回即字符串） |
| **代码原文（问题点）** | ```js
// server.js 第1146-1151行（完整原文）
if (customization) {
    const colNames = CUSTOMIZATION_COLUMNS.join(',')
    const placeholders = CUSTOMIZATION_COLUMNS.map((_, i) => `$${i + 2}`).join(',')
    await tx.$executeRawUnsafe(
        `INSERT INTO public."SchoolCustomization" (id, school_code, ${colNames}, updated_at)
         VALUES ($1,$2,${placeholders},now())`,
        crypto.randomUUID(), bin.original_code,
        ...CUSTOMIZATION_COLUMNS.map((c) => customization[c] ?? null)  // ← 缺 ::jsonb
    )
}
``` |
| **对照基准（正确写法）** | `server.js:1256` PUT customization：`sets.push(`\`${col}" = $${params.length}::jsonb\``)` ✅ |
| **DDL 分歧（新发现）** | 基线 migration (`20260726000000_baseline/migration.sql:184-200`) 声明这些列为 **TEXT**，但生产 `public` schema 实际为 **jsonb**（`tenantSync.js:91-93` 注释佐证："public schema 历史遗留 visible_types 为 jsonb 类型"）。同一段代码在不同 schema 上表现不同 |
| **影响面（全仓库扫描结果）** | backend/ 下所有 `$executeRawUnsafe`/`$queryRawUnsafe` 涉及 SchoolCustomization jsonb 列写入的点：**共 8 处**，其中 **7 处已有 `::jsonb` cast**（server.js:935/953/966/1256, tenantSync.js:95/105/114），**仅 1 处缺失** → server.js:1146-1151（restore INSERT） |
| **修复方案** | 将第 1147-1148 行的 placeholders 改为带 cast 版本：`\`${CUSTOMIZATION_COLUMNS.map((_, i) => \`$${i + 3}::jsonb\`).join(',')}\`` ；同时将 map 内的值确保为 JSON 字符串：`customization[c] ?? null` 若为对象则 `JSON.stringify()` |
| **回滚方案** | git revert 即可；已恢复的学校不受影响（恢复成功后不再走此路径） |
| **验证用例** | ① 创建测试学校 → 删除至回收站 → 点"恢复" → 应成功不再报 42804 ② 恢复后检查 SchoolCustomization 表各 jsonb 列数据完整性 ③ 对 TEXT 类型的租户 schema 执行同样操作确认兼容性 |

---

### FIX-09：R03 — 数据同步无取消按钮

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `R03/1786606688886_d74c7b8f.png` + `R03/1786606736593_11a07ff1.png` 显示备份恢复页面有三个操作按钮但无取消/关闭入口。与描述一致 ✅ |
| **根因置信度** | **中（UI 缺陷，代码逻辑清晰）** |
| **根因代码定位** | `js/modules/BackupRestore.js`：`handleCloudRestore()`(:234-236) 直接执行无取消模态框；`pauseSync()`(:252-254) 静默执行；`clearLocalCache()`(:272-274) 仅浏览器 `confirm()` |
| **修复方案** | 为 handleCloudRestore 增加 Modal 进度对话框 + 取消按钮（调用 sync cancel 端点）；pauseSync 增加二次确认模态框 |
| **回滚方案** | git revert |
| **验证用例** | 点击"云端同步"→ 弹出进度对话框 → 点取消 → 同步中止 → 页面回到初始状态 |

---

### FIX-10：R06 — 病原体检测下载模板打不开

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `R06/1786611384178_b9386c79.png` 显示 Word 报错"文件损坏无法打开 `pathogen_template (4).docx`"。与描述一致 ✅ |
| **根因置信度** | **中偏低（上一版分析部分有误）** |
| **修正说明** | 上一版称模板文件"不存在于代码库"。**经本次全仓库搜索确认 `pathogen_template.docx` 实际存在于项目根目录**（也可能是构建后拷贝到 dist/）。文件存在但 Word 报"损坏"，说明问题是**文件内容本身损坏/格式不合法**，而非 404 |
| **根因代码定位** | `js/modules/Pathogen.js:125-173` HEAD 探测 + 下载逻辑。若文件存在但内容异常（如被 Git LFS 占位符替换、或编码损坏），则下载后 Word 无法解析 |
| **修复方案** | ① 用有效 Word 文档重新生成/替换 `pathogen_template.docx` ② 在前端增加 Content-Type + 文件大小校验，异常时提示"模板文件异常请联系管理员" |
| **回滚方案** | 替换回原文件即可 |
| **验证用例** | 点击"下载模板" → 浏览器下载 .docx 文件 → 用 Microsoft Word / WPS 打开 → 应正常显示模板内容 |

---

### FIX-11：R07 — 界面定制果蔬检测项目删不掉

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `R07/1786611424894_4fda8869.png` 显示界面定制弹窗中下拉选项有 × 按钮，但用户反馈删不掉。与描述一致 ✅ |
| **根因置信度** | **中（需产品语义确认）** |
| **根因代码定位** | `admin-schools.html:2421` 果蔬 `batchNo` 是**内置字段**（`builtin:true`）；选项 chips 的 × 按钮代码层面可写 `fieldOptions[batchNo]`；但录入端 `js/utils/schoolCustomization/fields.js:182-201` 的 `applySchoolCustomizationToForm` 对内置字段可能内联了默认 `<option>` 导致定制覆盖失效 |
| **与 R19 关联** | R07（删不掉）和 R19（新增不生效）是**同一根因的两个面**：内置字段 `batchNo` 的选项修改在录入端未正确生效 |
| **修复方案** | 需产品确认：(a) 若允许自定义 → 确保录入端完全从 `fieldOptions[batchName]` 读取，不内联默认 option；(b) 若不允许 → 隐藏内置字段的选项编辑入口并加 tooltip 说明 |
| **验证用例** | 界面定制 → 果蔬农残检测项目 → 删除一个下拉选项 → 应用 → 回到检测页 → 确认选项已消失 |

---

### FIX-R19（新增）：R19 — 界面定制新增检测项目下拉选项无法添加

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `R19/1786685478958_2dbfe491.png` 显示界面定制弹窗中已添加下拉选项 `123`；截图 `R19/1786685492984_2582a339.png` 显示检测页面的检测项目下拉仍是默认选项（克百威等），**新增的 123 未出现**。与 REPORT.md "新增加项目无显示"一致 ✅ |
| **根因置信度** | **中（与 FIX-11 同源）** |
| **根因代码定位** | 同 FIX-11：内置字段 `batchNo` 的 `field_options` 修改在录入端 `applySchoolCustomizationToForm` 未正确覆盖默认值。新增选项和删除选项受同一机制管控 |
| **修复方案** | 同 FIX-11（合并处理） |
| **验证用例** | 界面定制 → 果蔬农残 → 输入类型选"下拉选择" → 添加新选项"测试项X" → 应用 → 回到检测页 → 检测项目下拉应包含"测试项X" |

---

### FIX-14：NEW-170443 — 超管平台修改账户类别失败

| 维度 | 结论 |
|---|---|
| **现象复核** | 截图 `NEW-170443/1786611893650_6bdfd99a.png` 显示编辑用户弹窗保存后底部红字"更新用户失败"，Network 显示 PUT 401 + 500。与描述一致 ✅ |
| **根因置信度** | **中（需运行态鉴权信息确认）** |
| **根因代码定位** | 前端：`admin-schools.html:3798-3845` PUT 到 `/api/admin/schools/${currentSchoolCode}/users/${id}`。后端：`server.js:1580` 挂载 `authenticateUser, requirePlatformSuperAdmin`；`authMiddleware.js:320-327` 要求 `role==='admin' && !schoolCode`（纯平台超管）。catch 块（`:1632-1638`）吞掉所有异常返回 500 |
| **双重故障分析** | **401** = 当前 token 不是纯平台超管（可能是 manager，token 带 schoolCode）；**500** = 即使鉴权通过，后续 `createTenantClient`/`$queryRawUnsafe` 异常被 catch 吞没 |
| **修复方案** | ① 排查编辑用户时的登录角色是否为平台超管 ② 拆分 catch 块为 400/403/404/409/500 分级错误码 ③ 前端对 401 提示"请重新以平台超管登录" |
| **回滚方案** | git revert |
| **验证用例** | 以平台超管登录 → 用户管理 → 编辑任一用户 → 修改类别 → 保存 → 应成功 |

---

### FIX-15：NEW-140845 + NEW-140219 — viewer 越权新增检测记录（2 合 1）

| 维度 | 结论 |
|---|---|
| **现象复核** | 6 张截图（`NEW-140845` 4张 + `NEW-140219` 2张）均显示 viewer 账号新增记录后"保存成功"但刷新后数据消失。与描述一致 ✅ |
| **根因置信度** | **高（调用链全程可验证）** |
| **安全分级（重要修正）** | **后端 ✅ 已安全拦截** — 所有写接口挂载 `requireEditorOrAbove`（`server.js:573-582`, `authMiddleware.js:518-528`），viewer → 403，数据库零写入。**前端 ❌ 漏防** — `GenericTest.handleSubmit`(:1130-1269) 无 `records:create` 前置校验；`Storage.save`(:101-128) 无条件写 localStorage + 弹"成功"提示；403 后 tempRecord 不回滚 |
| **实际风险等级** | **中（Medium）** — 非高危越权漏洞。影响：(1) UX 假成功造成用户困惑 (2) localStorage 残留脏数据污染缓存。不可达成持久化越权写入 |
| **代码原文（守卫）** | ```js
// server.js:574-581（完整原文）
function requireEditorOrAbove(req, res, next) {
    const role = req.user?.role ?? req.userRole
    if (!role || role === 'guest' || role === 'viewer') {
        return res.status(403).json({ error: '❌ 访客无写入权限，请以正式账号登录后操作' })
    }
    next()
}
``` |
| **影响面（全仓库扫描）** | 3 处保存入口均缺权限校验：`GenericTest.js:1233`、`Tableware.js:769`、`Pathogen.js:281`。全局搜索 `hasPermission('records:create')` → **0 处命中** |
| **修复方案** | ① `GenericTest.js handleSubmit` 入头加 `if (!permissionService.hasPermission('records:create')) { error; return }` ② `Tableware.js`/`Pathogen.js` 同上 ③ `Storage._processQueuedRequests` 的 403 分支回滚本地 tempRecord 缓存 |
| **回滚方案** | git revert |
| **验证用例** | ① viewer 账号登录 → 检测页 → 确认看不到"+ 添加检测点位"按钮（或点击后提示"权限不足"） ② viewer 尝试通过浏览器 DevTools 直接调用 POST /api/test-records → 应返回 403 ③ editor/operator 账号正常新增不受影响 |

---

### FIX-17：NEW-170648 + NEW-170647 — 洗涤剂残留识别不准（2 合 1）

| 维度 | 结论 |
|---|---|
| **现象复核** | 6 张截图显示比色卡+离心管拍摄图像，算法标注了色块检测框和离心管区域。色块定位偏移、液面采样不准、浓度判定与目视不一致。与描述一致 ✅ |
| **根因置信度** | **中（算法局限性，非单点 bug）** |
| **根因代码定位** | `js/utils/detergentColorimetry.js` 纯 Canvas 数值计算（无 ML 模型）。饱和度显著度峰检测（SAT_THRESH=0.04）对光照敏感；离心管四周搜索垂直长条高饱和区易受背景干扰；ΔE76 匹配经验阈值未校准批次差异 |
| **修复方案** | 短期：增加拍摄引导 UI（框选区域/亮度提示/角度校正）；中期：白平衡校正 + 模板匹配替代饱和度峰值 + 低置信度时提示重拍；长期：接入云端 AI 视觉 API |
| **预估工作量** | 3-5 天（算法改进类） |
| **验证用例** | 固定光照/角度/距离条件下拍摄标准比色卡 → 算法判定浓度级别 → 与目视/仪器比对 → 误差 ≤ 1 级 |

---

## 五、本轮新发现问题

| 编号 | 问题 | 发现方式 |
|---|---|---|
| **NEW-FIND-1** | **REPORT.md 顶部汇总数字与 snapshot.json 不一致**（通过23 vs 28，失败17 vs 20）— report-sync 生成脚本统计口径可能有 bug | Gate-3 计数勾稽 |
| **NEW-FIND-2** | **上一版漏掉 R19**（failed-open 项应为 17 个而非 16 个）— R19 "界面定制新增检测项目下拉选项无法添加"是独立待修复项 | 状态矩阵全量核对 |
| **NEW-FIND-3** | **FIX-04 置信度应下调为"低"** — 代码中红框逻辑完整闭合，Tailwind 预编译产物包含所需 CSS 类，工单描述可能与代码现状不同步 | 源码验证 |
| **NEW-FIND-4** | **FIX-07 置信度应下调为"低"** — `003_backup-now.mjs` 文件实际存在，上一版搜索遗漏 .mjs 后缀导致误判 | 全量文件搜索 |
| **NEW-FIND-5** | **FIX-10 根因需修正** — 模板文件存在但内容可能损坏（非 404），修复方向从"移除按钮"改为"替换有效文件" | 全仓库搜索 |
| **NEW-FIND-6** | **DDL 分歧风险** — SchoolCustomization 的 jsonb 列在基线 migration 声明为 TEXT 但生产 public schema 实际为 jsonb，同一段 SQL 在不同 schema 表现不同 | DDL 审查 |

---

## 六、与上一版文档的差异对照表

| 差异类型 | 条目 | 修正原因 |
|---|---|---|
| **新增** | **FIX-R19**（界面定制新增检测项目下拉选项无法添加） | 上一版漏做状态矩阵全量核对，遗漏 R19 这个 failed-open 项 |
| **删除（降级）** | **FIX-01**（Q1复检阶段A不刷新）→ 移入"已收口清单" | 上一版未检查 closed 字段，将 renkang 已收口的 failed 项误列待修复 |
| **删除（降级）** | **FIX-02**（V1肉蛋定制报错500）→ 移入"已收口清单" | 同上 |
| **修正-置信度下调** | **FIX-04** V7红框：高 → **低** | 源码验证发现代码逻辑完整闭合，Tailwind CSS 类存在，工单描述可能过时 |
| **修正-置信度下调** | **FIX-07** B9定时任务：中 → **低** | 全量搜索发现 `003_backup-now.mjs` 实际存在，上一版搜索遗漏 .mjs 后缀 |
| **修正-根因** | **FIX-10** R06模板打不开："文件不存在(404)" → **"文件存在但内容损坏"** | 全仓库搜索确认 pathogen_template.docx 存在于仓库 |
| **修正-安全评级** | **FIX-15** viewer越权：高危 → **中** | 后端确认已 403 安全拦截，前端仅为 UX 假成功缺陷，不构成持久化越权 |
| **新增发现** | **NEW-FIND-1~6** | 本轮 Gate 流程 + 源码深度核查发现 |
| **计数修正** | 待修复失败项：16 → **17** | 补入 R19 |

---

## 七、最终修复优先级（修订版）

### P0 — 立即修复（数据完整性/确定根因）

| 编号 | 问题 | 置信度 | 预估工作量 | 依赖 |
|---|---|---|---|---|
| **FIX-08** | R01/R15 学校恢复 jsonb 类型错误 | **高** | 0.5h | 无（参照同文件已有正确写法） |
| **FIX-15** | viewer 越权新增检测记录（前端漏防） | **高** | 1h | 无（前端加权限检查即可） |
| **FIX-14** | 超管修改账户类别 401/500 | **中** | 2h | 需排查鉴权态+细化错误码 |

### P1 — 本周修复（核心功能/高置信度）

| 编号 | 问题 | 置信度 | 预估工作量 | 依赖 |
|---|---|---|---|---|
| **FIX-03** | 帮助中心跳转失败（V6/R13/回归V6） | **高** | 1h | Caddy 配置变更（运维配合） |
| **FIX-06** | B7 旧 schema 未清理 | **高** | 1h | 生产环境验证 DROP 逻辑 |
| **FIX-11+R19** | 界面定制内置字段选项修改不生效 | **中** | 1.5h | 产品确认内置字段策略 |

### P2 — 待补证后定级（置信度低/证据不足）

| 编号 | 问题 | 置信度 | 阻塞项 |
|---|---|---|---|
| **FIX-04** | V7 非法输入无红框 | **低** | 需复现确认（代码显示逻辑正确） |
| **FIX-07** | B9 定时任务/告警 | **低** | 需 SSH 登录查看 systemctl journalctl 日志 |
| **FIX-09** | R03 同步无取消按钮 | **中** | 无阻塞 |
| **FIX-10** | R06 模板打不开 | **中偏低** | 需替换有效的 .docx 文件 |
| **FIX-17** | 洗涤剂识别不准 | **中** | 算法改进，工作量大 |

---

*复核完成。所有引用代码均为 snapshot 对应版本的原文片段，行号为精确行号。*
